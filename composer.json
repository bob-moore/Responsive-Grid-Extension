{
    "name": "bmd/responsive-grid-extension",
    "description": "Extension for core/grid wordpress block to use responsive grid attributes.",
    "type": "library",
    "version": "0.1.1",
    "license": "GPL-2.0-or-later",
    "minimum-stability": "dev",
    "prefer-stable": true,
    "config": {
        "optimize-autoloader": true,
        "vendor-dir": "vendor",
        "platform": {
            "php": "8.1"
        },
        "allow-plugins": {
            "composer/installers": true,
            "dealerdirect/phpcodesniffer-composer-installer": true
        }
    },
    "autoload": {
        "psr-4": {
            "Bmd\\": "./inc"
        }
    },
    "require-dev": {
    },
    "scripts": {
    }
}
